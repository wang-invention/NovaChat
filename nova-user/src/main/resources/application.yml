server:
  port: 8081
  servlet:
    context-path: /user

management:
  endpoints:
    web:
      exposure:
        include: "health,info"
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.wang.novachat: debug
